# Centralized Agent Rules

Canonical project agent instructions are centralized in `.agents/AGENTS.md`.

Use `AGENTS.md` as the single source of truth for:
- response style requirements
- documentation/summaries location rules
- pipeline and path/config conventions

This file exists only as a Claude entrypoint and intentionally duplicates nothing.
